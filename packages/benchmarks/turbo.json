{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src", "workflows"],
      "outputs": [".well-known", "dist"]
    },
    "bench": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
